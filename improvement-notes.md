duo date and priority override options to be removable for projects that dont need them
improved override logic with os system integration, should be generally set to automatic with side option to override
v2 features: momentum view, "what to work on next" recommendation based on due date priority and what else?, cross-project time tracking/claude token tracking. make the inbox useful somehow not sure how
